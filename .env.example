# Optional, non-secret build metadata displayed by the application.
# Leave empty to omit the build label.
VITE_BUILD_LABEL=

# Endpoint URLs, credentials, and service keys are intentionally unsupported.
# Do not add secrets to VITE_* variables because Vite exposes them to the client bundle.